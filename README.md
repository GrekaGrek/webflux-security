# Auth REST API with Spring Security and WebFlux

App security logic:

1. Flow:
- User registration with credentials
- checking in DB that credentials matches and returning JWT 

2. Flow:
- received token validation in API